<?php

// Await for output: 1, -5, -22, 5

$numbers = [-1, 5, 22, -5];

function inverse(int $number) : int
{
    // Implement this method
}

foreach($numbers as $number) {
    echo inverse($number);
}
